const readlineSync = require('readline-sync')

/* function changeColorToComplementary(input){
    
    let inputParsed = input.match(/[0-9a-f]+/gi).join();
    let myColor =  Number(inputParsed).toString(16);
    console.log(myColor.toUpperCase());
    // returns hexcode
    console.log(("0x" + "000000".substring(0, 6 - myColor.length) + myColor.toUpperCase()))
}

*/

changeColorToComplementary(); 
